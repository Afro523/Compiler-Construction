//WriteStatement = Expressions expressions;
public class WriteStatement extends Statement{
Expressions expressions;

public WriteStatement(Expressions expressions){
  this.expressions = expressions;
}
}

